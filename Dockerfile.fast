FROM node:20-slim

COPY package.json package-lock.json* ./
RUN npm ci --prefer-offline --no-audit --no-fund --ignore-scripts

COPY . .
RUN npm run build

EXPOSE 8080

CMD ["npm", "start"]

